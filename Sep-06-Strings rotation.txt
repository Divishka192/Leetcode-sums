Java
class Solution {
    public int countRotations(String s, int k) {
        int n = s.length();
        int validRotationsCount = 0;

        // Check each possible cyclic rotation starting at shift index i
        for (int i = 0; i < n; i++) {
            int score = 0;

            // Count adjacent matching characters for the current rotation
            for (int j = 0; j < n - 1; j++) {
                char current = s.charAt((i + j) % n);
                char next = s.charAt((i + j + 1) % n);

                if (current == next) {
                    score++;
                }
            }

            if (score == k) {
                validRotationsCount++;
            }
        }

        return validRotationsCount;
    }
}
Complexity

Time: O(n 
2
 ) — Checks each of the n rotations by iterating through n−1 adjacent pairs.

Space: O(1) — Uses standard variables with modular arithmetic, avoiding extra memory allocation or string concatenation.

Cound u give me solution in text

Here is the solution text so you can easily copy and paste it:

Java
class Solution {
    public int countRotations(String s, int k) {
        int n = s.length();
        int validRotationsCount = 0;

        // Check each possible cyclic rotation starting at shift index i
        for (int i = 0; i < n; i++) {
            int score = 0;

            // Count adjacent matching characters for the current rotation
            for (int j = 0; j < n - 1; j++) {
                char current = s.charAt((i + j) % n);
                char next = s.charAt((i + j + 1) % n);

                if (current == next) {
                    score++;
                }
            }

            if (score == k) {
                validRotationsCount++;
            }
        }

        return validRotationsCount;
    }
}
Not ans question

Could you clarify what you need help with? Let me know if you would like an explanation of how the logic works, an optimized O(n) solution, or code written in a different programming language.

This question in text

Certainly! Based on the images you provided of the LeetCode Weekly Contest 518 problem, here is the full problem description, example cases, and constraints in text format.

Problem Description
Q1. Count Rotations With Exactly K Equal Adjacent Pairs
(Easy, 3 pt.)

You are given a string s of length n and an integer k.

A cyclic rotation of s is obtained by choosing a prefix of s whose length is between 0 and n - 1 (inclusive), and moving it to the end of the string while preserving the order of all characters.

For every cyclic rotation of s, let its score be the number of indices i such that 0 <= i < n - 1 and the characters at positions i and i + 1 are equal.

Return the number of cyclic rotations of s whose score equals k.

A prefix of a string is a substring that starts from the beginning of the string and extends to any point within it.

A substring is a contiguous sequence of characters within a string, which may be empty.

Examples
Example 1:

Input: s = "aab", k = 1

Output: 2

Explanation:
The cyclic rotations of s are:

"aab": The characters at positions 0 and 1 are equal ('a' == 'a'), so score = 1.

"aba": No two adjacent characters are equal, so score = 0.

"baa": The characters at positions 1 and 2 are equal ('a' == 'a'), so score = 1.

Since score equals k for 2 cyclic rotations of s, the answer is 2.

Example 2:

Input: s = "abca", k = 0

Output: 1

Explanation:
The cyclic rotations of s are:

"abca": No two adjacent characters are equal, so score = 0.

"bcaa": The characters at positions 2 and 3 are equal ('a' == 'a'), so score = 1.

"caab": The characters at positions 1 and 2 are equal ('a' == 'a'), so score = 1.

"aabc": The characters at positions 0 and 1 are equal ('a' == 'a'), so score = 1.

Since score equals k for only 1 cyclic rotation of s, the answer is 1.

Constraints
1 <= s.length <= 100

s consists of lowercase English letters.

0 <= k < s.length