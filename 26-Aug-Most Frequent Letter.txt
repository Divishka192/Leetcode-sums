Most Frequent Letter
You are given a string 
S
S containing uppercase and lowercase English letters, digits, spaces, and special characters.

Only the alphabetic characters in the string are considered. Letter case is ignored, so uppercase and lowercase forms of the same letter are treated as equal. For example, A and a represent the same letter.

Find the letter that appears most frequently in 
S
S.

If multiple letters have the same maximum frequency, choose the one that comes first alphabetically.

Print the answer as a lowercase letter.

Input Format
The first line contains the string 
S
S.
Output Format
Print a single lowercase letter — the most frequent alphabetic character in 
S
S.
Constraints
1
≤
∣
S
∣
≤
10
5
1≤∣S∣≤10 
5
 
S
S may contain uppercase and lowercase English letters, digits, spaces, and special characters.
S
S contains at least one English alphabetic character.
Sample 1:
Input
Output
Hello, World! 123
l
Explanation:
Ignoring non-alphabetic characters and letter case, the string becomes helloworld.

The letter l appears 
3
3 times, which is more than any other letter.

Therefore, the answer is l.

Sample 2:
Input
Output
zzYYxx
x
Explanation:
After ignoring case, the letters x, y, and z each appear 
2
2 times.

Since x comes first alphabetically, the answer is x.



import java.util.*;

class Codechef {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String s = sc.nextLine();

        int[] count = new int[26];

        for (int i = 0; i < s.length(); i++) {
            char ch = Character.toLowerCase(s.charAt(i));

            if (ch >= 'a' && ch <= 'z') {
                count[ch - 'a']++;
            }
        }

        int max = 0;
        char ans = 'a';

        for (int i = 0; i < 26; i++) {
            if (count[i] > max) {
                max = count[i];
                ans = (char)('a' + i);
            }
        }

        System.out.println(ans);
    }
}




Idea
Read the whole string.
Create an array of 26 counters.
For every character:
Convert uppercase to lowercase.
If it is a letter, increase its count.
Find the letter with the highest count.
